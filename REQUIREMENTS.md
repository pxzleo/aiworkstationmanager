# AI 工作站管理系统需求文档

> 文档状态：需求基线草案 v0.3
> 编写日期：2026-08-26
> 项目目录：`D:\AIWork\4090manager`
> 适用阶段：MVP 设计、开发与验收

> 现场盘点状态（2026-08-27）：正式控制入口、健康接口与安全阻断项见 `CONTROL_INVENTORY.md`；在其中标记的阻断项补齐并验收前，生命周期控制继续保持关闭。

## 1. 项目背景

当前 AI 工作站同时存在 Windows 原生环境、WSL2、Docker 容器、systemd 服务以及多个不同推理后端。不同模型的启动参数、GPU 绑定、端口、健康检查和日志位置分散，主要依赖命令行或各项目自带的独立控制页面。

本项目需要提供一个可通过局域网浏览器访问的统一管理系统，用来管理工作场景、工作环境和模型实例，监控整机与推理服务状态，并集中记录运行日志和操作记录。

本系统是“控制面”，不是新的推理引擎。它必须优先复用和管理已有的 NInfer、q27、vLLM、llama.cpp、ComfyUI 等服务，而不是强迫现有服务迁移到单一推理框架。

## 2. 建设目标

### 2.1 核心目标

1. 在一个 Web 界面中查看所有 AI 工作环境及其真实运行状态。
2. 安全地启动、停止、重启不同工作环境，并能查看操作进度和明确的失败原因。
3. 保存多个模型、运行后端和参数预设，支持在“开发/agent场景”和“视频制作场景”之间一键切换整套双 GPU 模型组合。
4. 实时监控 CPU、内存、磁盘、网络、GPU 负载、显存、温度、功耗及相关进程。
5. 集中查看应用日志、生命周期日志、系统事件和管理操作审计日志。
6. 支持在局域网内通过浏览器管理，同时具备登录认证和基础安全防护。
7. 不干扰未纳入管理或正在运行的现有 AI 服务。

### 2.2 成功标准

- 用户不进入终端即可完成日常环境启停、状态查看、参数切换和日志排查。
- 系统显示的状态与 Docker、systemd、进程及健康检查的真实状态一致。
- 系统能够提前发现 GPU、显存、端口、模型路径等常见冲突。
- 管理系统自身重启后能够恢复配置，并重新发现外部服务的真实状态。
- 任一控制操作均可追溯到执行人、时间、目标、结果和错误信息。
- 用户可在 Web UI 中选择目标工作场景，无需进入终端逐个停止、启动和检查模型服务。
- 场景切换失败时不得留下无法识别的半切换状态；系统必须明确显示失败步骤，并执行安全回滚或给出人工恢复指引。

## 3. 当前工作站环境基线

以下内容为 2026-08-26 的只读核对快照。运行状态和资源占用属于动态信息，开发和验收时必须重新检测，不能硬编码。

### 3.1 主机硬件与系统

| 项目 | 当前值 |
|---|---|
| 主机名 | `XUPC` |
| 操作系统 | Windows 11 专业版，版本 `10.0.26200`，Build `26200` |
| CPU | Intel Core i7-13700K，16 核 / 24 线程 |
| 物理内存 | 约 63.8 GB |
| C 盘 | 约 952.8 GB，总剩余约 30.4 GB |
| D 盘 | 约 1.79 TB，总剩余约 381.8 GB |
| 局域网 IPv4 | `192.168.100.190/24`，动态值 |

### 3.2 GPU

| GPU | 完整 UUID | 显存 | 快照占用 | 快照状态 |
|---|---|---:|---:|---|
| NVIDIA GeForce RTX 4090 | `GPU-24e90667-f02e-1e21-e5fa-b4bd6566ce63` | 49,140 MiB | 46,359 MiB | NInfer 4090 正在占用 |
| NVIDIA GeForce RTX 3090 | `GPU-3b71dd71-0d3f-6f92-8374-2f2b5f23ef8d` | 24,576 MiB | 0 MiB | 空闲 |

当前 NVIDIA 驱动版本为 `610.88`。GPU 序号可能随驱动、WSL 或设备枚举变化，所有持久化配置必须使用完整 GPU UUID；界面可同时显示便于阅读的 GPU 0/GPU 1，但不能只保存序号。

### 3.3 虚拟化与容器

| 项目 | 当前值 |
|---|---|
| WSL 发行版 | `Ubuntu-22.04`，WSL2，运行中 |
| Docker 后端 | Docker Desktop，Linux 容器 |
| Docker Server | `29.7.2` |
| Docker 可用资源 | 12 CPU，约 47.0 GiB 内存 |
| 容器数量 | 23 个：1 个运行，22 个停止 |

### 3.4 当前主要模型环境

| 环境 | 管理方式 | 当前状态 | 端口/接口 | 备注 |
|---|---|---|---|---|
| `ninfer-4090` | Docker Compose | 运行中 | `0.0.0.0:8080` | `/health`、`/v1/models`、`/slots`、`/metrics` 可用 |
| `ninfer-3090` | Docker Compose | 已停止 | 后端计划端口 `18030` | 容器 restart policy 为 `unless-stopped` |
| `ninfer3090-ui.service` | WSL systemd | 运行中 | `127.0.0.1:18031` | UI 存活，但 3090 后端当前停止 |
| `qwen38-27b-rtx3090-single-1` | Docker Compose | 已停止 | 未监听 | restart policy 为 `no` |
| q27 | WSL 原生构建/服务 | 当前未发现监听 | 历史上可作为独立推理后端 | 项目位于 `/home/xu/ai_stud/q27` |
| FastGPT 相关容器组 | Docker Compose | 已停止 | 未监听 | 包含应用、数据库、Redis、MinIO 等依赖 |
| Open WebUI / Qdrant | Docker | 已停止 | 未监听 | 属于历史 AI/RAG 环境 |
| ComfyUI 开发图像/视频服务 | Windows 原生 ComfyUI 固定 Python 入口 | 已配置，按需启动 | `127.0.0.1:8189` | 开发/agent场景固定 RTX 3090；Krea2 生图、MiniMax H3 生视频；与视频辅助 8001 使用独立用户目录和数据库 |
| ComfyUI 视频服务 | Windows 原生 ComfyUI | 已配置，当前未运行 | 计划 `127.0.0.1:8000` | 视频场景绑定 RTX 4090，运行 MiniMax H3 |
| ComfyUI 音频服务 | Windows PowerShell 启动的独立 ComfyUI | 已配置，当前未运行 | 计划 `127.0.0.1:8001` | 视频场景绑定 RTX 3090，运行 Qwen TTS、ACE-Step 1.5 等辅助模型 |

此外，WSL 当前监听 `127.0.0.1:3000` 的 Node 进程已确认是 `/home/xu/ai_stud/xz_server/netease-cloud-music-api/app.js`，与模型监控无关。系统应能显示其归属，但在没有对应环境定义时不得自动接管或停止。

### 3.5 当前 NInfer 4090 参数快照

当前运行模型为 `qwen3.8-27b`，支持视觉输入，关键参数包括：

- 最大上下文：262,144
- KV capacity：600,000
- 最大并发：8
- 最大等待请求：16
- KV 类型：INT8
- MTP speculative decoding：3 个 draft tokens
- reasoning effort：`xhigh`
- 模型目录以只读方式挂载到容器
- API 当前发布到 `0.0.0.0:8080`

这些值仅作为系统首批导入模板的来源，不应写死为产品默认值。

### 3.6 ComfyUI 与视频工作流基线

已确认存在以下本地资源：

- ComfyUI 工作目录：`C:\Users\xu\Documents\ComfyUI`
- 统一模型目录：`D:\ComfyUI\Models`
- OpenMontage 项目：`D:\AIWork\openmontage`
- RTX 3090 音频服务启动脚本：`C:\Users\xu\Documents\ComfyUI\audio-3090\Start-ComfyUI-Audio-3090.ps1`

此前视频工作流已验证的路由为：

- `127.0.0.1:8189` → RTX 3090 → 开发/agent场景 ComfyUI；Krea2 Turbo INT8 生图、MiniMax H3 FL2VA pruned INT8 + 8-step Turbo LoRA 生视频。使用独立 `user-image-3090` 数据库，必须以 `--cuda-device 1` 启动并通过物理 GPU UUID、Comfy 内部设备、Krea2/H3 节点与模型文件交叉检查。
- `127.0.0.1:8000` → RTX 4090 → ComfyUI MiniMax H3 视频生成。
- `127.0.0.1:8001` → RTX 3090 → ComfyUI Qwen TTS、ACE-Step 1.5 音乐生成，并为后续 ASR/其他视频辅助模型预留。
- H3 当前标准参数为 8 步采样，使用 `minimax_h3_fl2v_turbo_8step_v1.0_comfyui_bf16.safetensors`，`shift_video = 12`、`shift_audio = 3`；不得在场景模板中回退到 4 步工作流或搭配 4-step LoRA。

本轮在线检查时 8000 和 8001 均未监听，因此上述内容属于“已配置、曾验证的目标环境”，不能在 UI 中显示为当前运行中。管理系统必须通过 ComfyUI `/system_stats`、队列状态和 GPU 实际占用确认真实状态。

### 3.7 两种工作场景定义

#### 场景一：开发/agent场景

| GPU | 主要职责 | 环境/模型 |
|---|---|---|
| RTX 4090 | 主语言模型推理 | NInfer + Qwen3.8-27B，当前 API 端口 8080 |
| RTX 3090 | 开发辅助 AI 服务 | ComfyUI Krea2 生图 / H3 生视频（8189）；`Ubuntu-22.04` WSL 内的 SenseVoiceSmall ASR（18090，`/home/xu/ai_stud/xz_server/sensevoice-asr-api`）与 IndexTTS 1.5 vLLM TTS（6006，`/home/xu/ai_stud/xz_server/index-tts-vllm`）作为开发/agent场景必需服务 |

ASR 与 TTS 使用独立的 user systemd unit `sensevoice-asr-api.service`、`index-tts-vllm.service`，均固定 `CUDA_VISIBLE_DEVICES=1`。Windows 已有 `0.0.0.0:18090/6006` 到 WSL 的端口转发，服务必须监听 WSL `0.0.0.0`，端口冲突检查必须在 WSL 内完成，不能把 Windows portproxy 监听误判为服务占用。

#### 场景二：视频制作场景

| GPU | 主要职责 | 环境/模型 |
|---|---|---|
| RTX 4090 | 主视频生成 | ComfyUI + MiniMax H3，目标端口 8000 |
| RTX 3090 | 视频流辅助模型 | ComfyUI 音频/辅助服务，目标端口 8001；包括 Qwen TTS、ACE-Step 1.5、ASR 及视频工作流需要的其他模型 |

两个场景是整套资源编排模板，而不只是单个模型名称。场景切换必须同时处理 4090 和 3090 上的服务、模型驻留、端口、队列和健康状态。

### 3.8 现有启动脚本资产

桌面目录 `C:\Users\xu\Desktop\本地模型启动` 是当前本机 AI 框架启动入口清单。已只读确认以下 7 个入口：

| 入口 | 框架/用途 | 底层管理方式 | 主要端口 | GPU/冲突关系 |
|---|---|---|---|---|
| `4090-NInfer.cmd` | NInfer + Qwen3.8-27B + NInfer UI | WSL Docker Compose + user systemd | API 8080、UI 8081 | RTX 4090；与 q27 共用 8080；与 vLLM 互斥 |
| `4090-q27.cmd` | q27 Qwen3.8-27B Q6_K | WSL systemd | API 8080 | RTX 4090；与 NInfer、vLLM 互斥 |
| `4090-vLLM.cmd` | vLLM Qwen3.8-27B FP8 | WSL user transient systemd | API 8000 | RTX 4090；与 H3 ComfyUI 8000 端口及 GPU 冲突 |
| `3090-NInfer.cmd` | NInfer 3090 + NInfer UI | WSL Docker Compose + systemd | API 18030、UI 18031 | RTX 3090；与其他 3090 大模型服务互斥 |
| `3090-Qwen3090-Control.cmd` | Qwen3.8-27B 3090 服务 | Windows Docker Compose | API 18020 | RTX 3090；与 NInfer 3090 互斥 |
| `DualGPU-Llama-BF16.cmd` | llama.cpp Qwen3.8-27B BF16 | Windows PowerShell/原生进程 | API 1234 | RTX 4090 + RTX 3090，85:15；与双卡上的场景服务冲突 |
| `lmstudio监控.lnk` | LM Studio Web Monitor | 快捷方式 → `D:\AIWork\lmstudio_web_monitor\run.bat` | UI 8765、LM Studio API 1234 | 可加载/卸载 LM Studio 模型并查看输入输出日志 |

这些 `.cmd` 文件包含交互式菜单，不适合由 Web 服务直接作为长期子进程运行。管理系统应将其作为“发现和迁移来源”，提取底层 Start/Stop/Status 动作并转成非交互、白名单化的运行适配器。扫描和导入阶段不得执行脚本。

脚本目录与脚本内容会变化。系统需要保存来源路径和校验值，检测变化后提示重新审核，不能静默覆盖已审批的适配器定义。

### 3.9 现有监控 Web UI

| UI | 当前地址 | 快照状态 | 能力与注意事项 |
|---|---|---|---|
| NInfer 4090 UI | `http://127.0.0.1:8081` | 在线，后端 8080 在线 | 显示服务、模型、slots、KV、GPU、请求、日志等信息 |
| NInfer 3090 UI | `http://127.0.0.1:18031` | UI 在线，后端 18030 离线 | 必须显示 UI 与模型后端的分离状态 |
| LM Studio Web Monitor | `http://127.0.0.1:8765` 或局域网地址 | 当前未监听 | 可查看 CPU、内存、GPU、显存、模型输入输出和日志，并执行模型加载/卸载 |

NInfer UI 当前只监听 loopback，局域网浏览器不能直接访问；LM Studio Web Monitor 当前实现会监听全部网卡、创建防火墙放行规则，并使用 URL 查询参数形式的静态访问凭据。管理系统不得把该凭据暴露在菜单、审计、Referer 或日志中。MVP 应通过管理系统的登录态和同源反向代理安全地打开现有 UI，或在管理员确认后采用经过加固的独立局域网地址。

NInfer 4090 UI 曾把 LAN API 地址写死为历史地址 `192.168.100.152`。2026-08-27 已移除 unit 和桌面脚本中的静态覆盖；快照保留 loopback API，前端根据当前浏览器 hostname 动态生成局域网地址，不能再将 WSL/Docker 历史网关地址作为 LAN 事实来源。

## 4. 调研结论与产品定位

前期调研的参考项目包括：

- [GPUStack](https://github.com/gpustack/gpustack)：参考 GPU 调度、权限、监控和多节点管理。
- [llama-swap](https://github.com/mostlygeek/llama-swap)：参考多模型按需加载、后端命令适配、日志流和统一 API。
- [LLM-Dock](https://github.com/teo-mateo/llm-dock)：参考 Docker Compose 环境创建、参数模板和模型扫描。
- [Spark Dashboard](https://github.com/niklasfrick/spark-dashboard)：参考主机、GPU 和 vLLM 指标展示。
- [LocalAI](https://github.com/mudler/LocalAI) / [Xinference](https://github.com/xorbitsai/inference)：参考模型注册、引擎选择和运行参数表单。

本项目不直接复制某个项目的完整架构。MVP 采用“轻量控制面 + 运行适配器”的方向，优先适配当前工作站上的 Docker Compose、Docker 容器、WSL systemd 和 OpenAI 兼容服务。

## 5. 用户与使用场景

### 5.1 MVP 用户

- 工作站管理员：配置环境、执行启停、修改参数、查看全部日志和审计信息。
- 只读观察者为后续版本候选；MVP 是否需要多用户角色仍待确认。

### 5.2 典型场景

1. 管理员在手机或另一台电脑上打开局域网 Web 页面，查看双卡负载。
2. 点击“视频制作场景”，系统安全排空并停止 4090 上的 NInfer，确认显存释放后启动 4090 ComfyUI H3，同时准备 3090 视频辅助服务。
3. 点击“开发/agent场景”，系统停止 H3 视频服务并释放显存，恢复 4090 NInfer/Qwen3.8，同时准备 3090 ComfyUI 生图/视频、ASR 和 TTS 服务。
4. 为同一个模型选择不同上下文、并发、KV 类型或推理后端预设。
5. 启动前检查目标 GPU 剩余显存、端口和依赖是否满足要求。
6. 服务启动失败时直接查看启动日志、健康检查和失败原因。
7. 查看最近一次参数修改、服务重启、场景切换和异常退出是谁在何时执行的。
8. 管理器重启后，重新发现已在外部运行的服务和当前实际场景，而不是重复启动。
9. 在环境卡片中点击“启动并打开监控 UI”，系统按需启动现有 NInfer 或 LM Studio 监控 UI，通过统一登录后的页面打开。
10. 从桌面脚本目录导入现有 AI 框架入口，审核其端口、GPU、依赖和互斥关系后纳入统一管理。

## 6. 范围定义

### 6.1 MVP 范围（P0）

- 单台 Windows + WSL2 双 GPU 工作站。
- Docker Compose、现有 Docker 容器、WSL systemd 服务和外部 HTTP 服务适配。
- 工作环境和参数预设管理。
- 开发/agent场景、视频制作场景的场景模板、状态识别和一键切换。
- 启动、停止、重启、健康检查和状态发现。
- CPU、内存、磁盘、网络、GPU、显存、温度、功耗监控。
- NInfer、ComfyUI 通用指标及 OpenAI 兼容接口健康检查。
- 实时日志、历史日志和操作审计。
- 中文 Web 管理界面和局域网登录认证。
- 导入当前 NInfer 4090、NInfer 3090、q27、ComfyUI 8000/8001、OpenMontage 及主要 Docker Compose 项目。
- 受控执行现有 ComfyUI PowerShell 启动/停止脚本；不提供任意命令执行能力。
- 只读扫描并人工确认导入 `C:\Users\xu\Desktop\本地模型启动` 中的 `.cmd`、`.bat`、`.ps1` 和 `.lnk`。
- 启动并打开 NInfer 4090/3090 现有监控 UI，以及 LM Studio Web Monitor。
- 为只监听本机的现有 UI 提供经过认证的同源代理，供局域网浏览器访问。

### 6.2 后续范围（P1/P2）

- 通用 Windows 服务和任意 Windows 原生进程适配。
- vLLM、llama.cpp、Ollama 等专用指标适配器。
- 模型文件扫描、校验、下载和版本管理。
- 多用户、角色权限、API Token。
- 告警通知、长期指标存储和报表。
- 多主机 Agent、远程工作站和统一调度。
- 跨场景自动调度、全局资源队列，以及无人确认的自动模型换入换出；MVP 只实现用户触发的场景切换和 3090 场景内部的安全排队/按需加载。
- 模型基准测试和结果对比。
- 移动端快捷操作和 PWA。

### 6.3 MVP 明确不做

- 不开发新的推理引擎。
- 不自动修改 NInfer、q27、vLLM 等项目源码。
- 不自动删除模型、容器、镜像、卷或历史日志。
- 不默认接管未知进程、未知容器或未知端口。
- 不在未确认的情况下切换当前运行模型或释放 GPU。
- 不包含整台工作站的关机、重启、Wake-on-LAN；是否加入后续版本待确认。
- 不直接向互联网暴露管理页面。

## 7. 核心领域模型

### 7.1 工作环境 Environment

工作环境是可独立管理的一组运行单元，至少包含：

- 唯一 ID、名称、说明、标签、分组。
- 运行适配器类型。
- 项目路径、工作目录、配置文件路径。
- 启动、优雅停止、强制停止、重启方式。
- 依赖服务及启动顺序。
- 使用的模型、GPU、端口和资源要求。
- 健康检查、就绪检查和超时。
- 日志来源。
- 自动启动策略。
- 当前状态、期望状态、最近一次错误。

### 7.2 工作场景 Scene

工作场景是一组需要协同切换的 Environment、Profile 和 GPU 资源分配，至少包含：

- 唯一 ID、名称、图标、说明和显示顺序。
- 每张 GPU 的职责、目标环境、模型和参数预设。
- 场景共享服务与场景专属服务。
- 服务停止、显存释放、启动和健康检查顺序。
- 任务排空策略、超时和强制切换权限。
- 目标端口、依赖、资源预算和互斥规则。
- 回滚场景和回滚步骤。
- 当前状态：`active`、`transitioning`、`partial`、`degraded`、`inactive`、`unknown`。

MVP 预置“开发/agent场景”和“视频制作场景”，但数据模型必须允许后续新增其他场景。

### 7.3 模型 Model

- 模型显示名称和 API 模型 ID。
- 本地路径或远程仓库标识。
- 格式，如 NInfer、GGUF、Safetensors。
- 模态，如文本、视觉、嵌入、重排、图像生成。
- 文件大小、校验值、只读共享状态。
- 推荐 GPU、显存要求和兼容后端。
- 模型来源、版本和备注。

### 7.4 参数预设 Profile

- 关联的后端和模型。
- 结构化参数：上下文、并发、KV、GPU、端口、批大小、量化等。
- 环境变量引用，但不直接保存明文密钥。
- 高级参数列表。
- 配置版本、创建人、修改人、修改时间和变更说明。
- 启动前可预览最终命令或 Compose 差异。

### 7.5 运行实例 Instance

- Environment 与 Profile 的一次实际部署。
- 真实进程、容器或 systemd unit 标识。
- 当前状态、PID/容器 ID、启动时间、健康状态。
- 实际绑定的 GPU UUID、端口和模型。
- 当前资源占用和最近一次操作。

### 7.6 操作任务 Operation

启动、停止和重启均作为异步任务记录：

- 操作 ID、类型、目标、发起人和发起时间。
- 当前阶段和进度消息。
- 执行结果、错误类型、原始错误和结束时间。
- 对配置、进程、容器和端口造成的变化。

### 7.7 外部 Web UI ExternalWebUI

- 唯一 ID、名称、所属环境和图标。
- 本机 URL、局域网 URL 或同源代理路径。
- 启动、停止和状态检查动作；UI 生命周期与模型后端生命周期必须分开。
- 健康检查 URL、预期内容和启动超时。
- 是否具有状态修改能力，例如 LM Studio 模型加载/卸载。
- 访问方式、代理要求、敏感凭据引用和安全状态。
- 支持的代理能力：普通 HTTP、流式响应、SSE、WebSocket 和相对静态资源。
- 当前 UI 状态、关联后端状态和最后检查时间。

### 7.8 脚本来源 ScriptSource

- 绝对路径、文件类型、目标快捷方式和工作目录。
- SHA-256、最后修改时间和最近审核时间。
- 解析出的项目路径、端口、GPU、健康检查和动作候选。
- 导入状态：`discovered`、`needs_review`、`approved`、`changed`、`rejected`。
- 管理员批准后的白名单动作映射；不得保存为浏览器可编辑的任意命令。

## 8. 功能需求

### 8.1 总览仪表盘

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-DASH-001 | 展示主机在线状态、运行时间、CPU、内存、磁盘和网络概况。 | P0 |
| FR-DASH-002 | 分卡展示 GPU 名称、UUID、负载、显存、温度、功耗、时钟和风扇。硬件不支持的指标显示“不支持”，不能伪造为 0。 | P0 |
| FR-DASH-003 | 展示运行中、启动中、异常和已停止的环境数量。 | P0 |
| FR-DASH-004 | 展示每个 GPU 上已识别的模型实例及无法识别的占用进程。 | P0 |
| FR-DASH-005 | 展示磁盘空间不足、GPU 温度过高、服务异常等告警。 | P0 |
| FR-DASH-006 | 页面断线、数据过期或采集失败时必须显式提示，不得继续显示成实时正常状态。 | P0 |
| FR-DASH-007 | 首页显著显示当前实际场景、目标场景、场景完整度和“一键切换”入口。 | P0 |
| FR-DASH-008 | 每个受管环境显示“打开监控 UI”“启动 UI”“启动环境并打开 UI”等适用动作，动作不可混淆。 | P0 |
| FR-DASH-009 | 总览页每张 GPU 卡片必须在卡片中部、显存信息右侧的大区域展示该卡独立的短时负载曲线；当前负载数值保留在右上角，曲线末端与当前负载保持一致。 | P0 |

### 8.2 工作场景管理与切换

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-SCN-001 | MVP 内置“开发/agent场景”和“视频制作场景”两张场景卡片，显示每张 GPU 的目标模型组合和当前实际状态。 | P0 |
| FR-SCN-002 | 用户点击场景卡片后先生成切换计划，列出将停止、保留、启动和重新加载的服务，不立即执行。 | P0 |
| FR-SCN-003 | 用户确认后以一次异步场景任务执行切换，全程显示当前步骤、耗时和日志。 | P0 |
| FR-SCN-004 | 切换前检查活跃推理请求、ComfyUI 队列、GPU 显存、模型文件、端口、启动脚本和依赖。 | P0 |
| FR-SCN-005 | 有活跃任务时默认禁止直接停止；允许“等待任务完成后切换”，管理员强制切换必须二次确认并审计。 | P0 |
| FR-SCN-006 | 执行顺序必须为：阻止目标环境接收新任务、排空源任务、停止源场景专属服务、验证显存释放、启动目标服务、验证健康和 GPU 绑定。 | P0 |
| FR-SCN-007 | 场景切换不得修改 Docker restart policy、systemd enable 状态或开机启动设置，除非用户单独执行对应配置操作。 | P0 |
| FR-SCN-008 | 目标场景启动失败时，停止本次新启动且不健康的组件，并在安全可行时恢复切换前场景；无法自动恢复时显示逐步人工恢复指引。 | P0 |
| FR-SCN-009 | 场景是否激活必须根据实际服务、模型、端口、健康检查和 GPU UUID 计算，不能仅使用数据库中的目标场景字段。 | P0 |
| FR-SCN-010 | 用户手工改变某个服务后导致实际状态偏离场景模板时，场景显示 `partial`，并展示缺失、多余和错误绑定的组件。 | P0 |
| FR-SCN-011 | 场景切换按钮的一次确认即可启动完整流程，不要求用户进入终端；模型冷启动时间按实际进度展示，不承诺不现实的固定秒数。 | P0 |
| FR-SCN-012 | 场景切换必须作为一个整体审计事件，同时记录每个子操作及回滚结果。 | P0 |
| FR-SCN-013 | 开发/agent场景中，4090 必须运行 NInfer/Qwen3.8；3090 由同一独立 ComfyUI 实例同时提供生图和生视频能力，并负责 ASR、TTS；无法同时驻留的模型使用队列或按需加载。 | P0 |
| FR-SCN-014 | 视频制作场景中，4090 必须运行 8000/H3；3090 必须运行 8001 视频辅助服务，支持 TTS、音乐、ASR 和其他已登记模型。 | P0 |
| FR-SCN-015 | H3 场景预设必须固定为已批准的 8-step 配置和兼容 LoRA，禁止静默回退到 4-step 路由。 | P0 |

### 8.3 环境管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-ENV-001 | 支持创建、查看、编辑、复制、禁用和归档工作环境。 | P0 |
| FR-ENV-002 | 删除环境配置前必须二次确认，且默认只删除管理配置，不删除项目目录、模型、容器、镜像或卷。 | P0 |
| FR-ENV-003 | 支持从已存在的 Docker Compose 项目、容器和 systemd unit 导入环境。 | P0 |
| FR-ENV-004 | 发现未知进程或服务时只显示，不自动接管。管理员明确确认后才能转为受管环境。 | P0 |
| FR-ENV-005 | 支持环境依赖关系和顺序，例如数据库就绪后再启动应用。 | P0 |
| FR-ENV-006 | 支持导出和导入不含密钥的环境定义。 | P0 |
| FR-ENV-007 | 支持从已批准的脚本来源创建环境草稿，并要求管理员确认解析出的动作、端口、GPU 和互斥关系。 | P0 |

### 8.4 运行适配器

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-ADP-001 | Docker Compose 适配器支持项目发现、配置检查、启动、停止、重启、状态和日志。 | P0 |
| FR-ADP-002 | Docker 容器适配器支持既有容器的启停、重启、状态和日志。 | P0 |
| FR-ADP-003 | WSL systemd 适配器支持指定发行版和 unit 的启停、重启、状态和 journal 日志。 | P0 |
| FR-ADP-004 | 外部 HTTP/OpenAI 服务适配器只执行健康检查和监控，默认不具备停止权限。 | P0 |
| FR-ADP-005 | Windows 服务适配器支持服务状态、启动、停止和事件日志。 | P1 |
| FR-ADP-006 | MVP 支持通过白名单模板管理现有 Windows ComfyUI 8000/8001/8189 固定 Python 入口，不允许前端提交任意 Shell 命令。 | P0 |
| FR-ADP-007 | 每个适配器必须返回明确的类型化错误，保留底层错误原因，禁止静默忽略。 | P0 |
| FR-ADP-008 | 通用 Windows 原生进程适配器属于 P1，同样必须使用受控模板。 | P1 |
| FR-ADP-009 | ComfyUI 适配器支持 `/system_stats`、队列、历史任务、模型释放和服务日志；缺少接口时明确标记能力缺失。 | P0 |

### 8.5 脚本目录发现与导入

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-IMP-001 | 默认扫描目录为 `C:\Users\xu\Desktop\本地模型启动`，管理员可增加其他明确目录；禁止扫描整个磁盘。 | P0 |
| FR-IMP-002 | 扫描只读取 `.cmd`、`.bat`、`.ps1`、`.lnk` 的元数据和文本，不执行任何脚本、快捷方式或脚本引用的程序。 | P0 |
| FR-IMP-003 | 安全解析 `.lnk` 的目标、参数和工作目录，并将目标脚本纳入同一次审核。 | P0 |
| FR-IMP-004 | 解析项目路径、WSL 发行版、Docker Compose 服务、systemd unit、API/UI 端口、健康地址、GPU 选择和日志路径。 | P0 |
| FR-IMP-005 | 识别交互式菜单脚本，禁止直接作为 Web 后台命令运行；需提取并重建非交互 Start/Stop/Status 动作。 | P0 |
| FR-IMP-006 | 导入向导显示原脚本、解析结果、无法理解的行和风险提示；存在未解析关键逻辑时不得自动批准。 | P0 |
| FR-IMP-007 | 自动检测已知端口和 GPU 冲突，包括 8080、8000、1234 及 3090 大模型互斥关系。 | P0 |
| FR-IMP-008 | 保存脚本 SHA-256 和最后修改时间；脚本变化后将来源标记为 `changed`，暂停自动采用新动作直至重新审核。 | P0 |
| FR-IMP-009 | 检测脚本中的静态 IP、凭据、宽泛防火墙规则和任意命令风险；敏感值只显示“已发现”，不得回显明文。 | P0 |
| FR-IMP-010 | 导入只新增管理配置，不移动、重命名或修改桌面脚本及其目标项目。 | P0 |

### 8.6 生命周期控制

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-LIFE-001 | 支持启动、优雅停止、重启；强制停止必须作为独立高风险操作。 | P0 |
| FR-LIFE-002 | 操作前执行预检：路径、配置、依赖、GPU、显存、端口、磁盘空间和权限。 | P0 |
| FR-LIFE-003 | 启动操作必须异步执行，界面持续显示拉取、创建、加载模型、监听端口、健康检查等阶段。 | P0 |
| FR-LIFE-004 | 重复点击同一操作必须幂等，不能重复创建容器或重复启动进程。 | P0 |
| FR-LIFE-005 | 停止正在处理请求的模型时，显示活跃请求和风险提示；优先等待排空，强制停止需二次确认。 | P0 |
| FR-LIFE-006 | 必须区分“停止当前实例”与“禁用自动启动/restart policy”。两个操作不能绑定执行。 | P0 |
| FR-LIFE-007 | 修改 restart policy 前展示旧值、新值和影响；同时检查运行与已停止容器。 | P0 |
| FR-LIFE-008 | 管理器启动后以真实外部状态为准进行状态协调，不根据上次数据库状态盲目重启服务。 | P0 |
| FR-LIFE-009 | 失败回滚仅恢复本次操作直接改变的状态，不影响无关服务。 | P0 |

### 8.7 状态模型

环境状态至少包含：

```text
unknown -> stopped -> starting -> loading -> ready
                    \-> degraded
                    \-> failed
ready -> stopping -> stopped
ready/degraded -> restarting -> starting
```

- `running` 不能等同于 `ready`。
- 容器/进程存在但健康检查失败时为 `degraded` 或 `failed`。
- 控制 UI 存活但模型后端停止时，必须分别显示“UI 在线”和“后端离线”。
- 状态必须包含检测来源和最后更新时间。
- 场景切换过程中，环境状态与场景状态必须分开显示；一个环境就绪不代表整个场景已激活。

### 8.8 GPU 与资源管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-GPU-001 | 持久化 GPU 配置时使用完整 UUID，并显示型号、显存和当前序号。 | P0 |
| FR-GPU-002 | 启动前根据实际占用和预估需求检查显存，不只依赖配置中的 GPU 数量。 | P0 |
| FR-GPU-003 | 显示 GPU 占用对应的容器、WSL 进程或 Windows 进程；无法归属时标记“未知占用”。 | P0 |
| FR-GPU-004 | 支持环境独占 GPU、允许共享 GPU和仅监控三种策略。 | P0 |
| FR-GPU-005 | 发现资源冲突时默认阻止启动；管理员可在看到冲突明细后强制继续并留下审计记录。 | P0 |
| FR-GPU-006 | 不得为了检查 GPU 状态而停止或重启任何现有服务。 | P0 |
| FR-GPU-007 | 开发/agent场景和视频场景均必须验证实际 GPU 归属；不能只因启动参数写了 GPU 序号就认为隔离成功。 | P0 |
| FR-GPU-008 | 3090 上的生图、生视频、TTS、音乐和 ASR 模型必须登记显存预算；未经兼容性验证不得默认同时常驻。 | P0 |

### 8.9 模型与参数管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-MOD-001 | 登记现有模型文件，记录路径、格式、大小、来源和关联环境。 | P0 |
| FR-MOD-002 | 同一模型文件可被多个环境只读引用，系统不得默认复制大模型。 | P0 |
| FR-MOD-003 | 参数采用结构化字段和高级附加参数组合，常用字段需提供说明和校验。 | P0 |
| FR-MOD-004 | 支持保存、复制、版本化和回滚参数预设。 | P0 |
| FR-MOD-005 | 修改参数不会自动重启服务；需要明确点击“应用并重启”。 | P0 |
| FR-MOD-006 | 应用前展示参数差异、最终 GPU、端口、挂载和启动命令摘要。 | P0 |
| FR-MOD-007 | 模型下载、删除、移动和校验属于 P1；删除必须可恢复或二次确认。 | P1 |
| FR-MOD-008 | 参数预设需支持场景覆盖：同一环境在开发/agent场景与视频场景中可使用不同 GPU、端口和模型组合。 | P0 |

### 8.10 监控

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-MON-001 | 采集 CPU 总负载、每核负载、内存、交换空间、磁盘空间、磁盘 I/O 和网络吞吐。 | P0 |
| FR-MON-002 | 采集每张 GPU 的利用率、显存、温度、功耗、时钟、风扇和节流状态。 | P0 |
| FR-MON-003 | 采集受管容器/进程的 CPU、内存、运行时间、退出码和重启次数。 | P0 |
| FR-MON-004 | NInfer 适配器读取 `/health`、`/v1/models`、`/slots` 和 `/metrics`。 | P0 |
| FR-MON-005 | 展示请求总数、活跃/排队请求、Token 吞吐、缓存命中和 KV 占用；后端不提供时显示“不支持”。 | P0 |
| FR-MON-006 | 实时页面建议 1～2 秒更新；历史数据默认每 5 秒采样，保留策略可配置。 | P0 |
| FR-MON-007 | 默认建议保留 24 小时原始数据和 30 天一分钟聚合数据，最终值待性能测试确认。 | P1 |
| FR-MON-008 | 采集器异常不得阻塞生命周期控制。 | P0 |
| FR-MON-009 | ComfyUI 监控展示当前设备、已加载模型、队列中/运行中任务、最近任务结果和显存释放状态。 | P0 |
| FR-MON-010 | 场景视图按 4090/3090 两列展示每个场景的职责、服务健康、队列和资源预算。 | P0 |
| FR-MON-011 | 资源监控页必须按物理 GPU 分区展示；RTX 4090 和 RTX 3090 各自使用独立曲线同时显示 GPU 负载与显存占用，禁止把两张卡聚合成一条曲线。 | P0 |
| FR-MON-012 | CPU 总负载和物理内存占用必须使用时间曲线展示；CPU 指标为整机总负载，不能用单个核心峰值替代。 | P0 |
| FR-MON-013 | 温度监控必须使用时间曲线同时展示 CPU、RTX 4090 和 RTX 3090 温度；无法读取 CPU 温度时明确显示“不支持”或“暂不可用”，不得以 0°C 代替。 | P0 |

### 8.11 日志与审计

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-LOG-001 | 统一展示容器日志、systemd journal、应用文件日志和管理器日志。 | P0 |
| FR-LOG-002 | 支持实时流、暂停、搜索、级别筛选、来源筛选和下载。 | P0 |
| FR-LOG-003 | 日志条目包含时间、环境、实例、来源、级别和正文。 | P0 |
| FR-LOG-004 | 对 API Key、Authorization、Cookie 和已知密钥字段进行脱敏。 | P0 |
| FR-LOG-005 | 日志达到大小或时间上限后轮转，不允许无限占用 C 盘。 | P0 |
| FR-AUD-001 | 记录登录、配置修改、启停、强制操作、自动启动策略修改和失败结果。 | P0 |
| FR-AUD-002 | 审计日志只追加，普通管理员不能在 Web 页面删除。 | P0 |
| FR-AUD-003 | 配置修改记录修改前后差异，但不记录密钥明文。 | P0 |

### 8.12 Web 界面

MVP 页面至少包括：

1. 登录页。
2. 总览仪表盘。
3. 工作场景页：开发/agent场景、视频制作场景、切换计划和切换进度。
4. 工作环境列表与详情页。
5. 新建/导入环境向导。
6. 模型清单与参数预设页。
7. 实时监控页。
8. 日志中心。
9. 操作任务和审计记录页。
10. 系统设置、采集器状态和诊断页。
11. 现有 Web UI 中心：NInfer 4090/3090、LM Studio Monitor 的状态、启动和打开入口。
12. 脚本来源页：扫描结果、解析差异、审核和重新导入。

界面要求：

- 默认中文，桌面与手机浏览器均可使用。
- 危险操作使用明确文案，不用含糊图标代替。
- 显示数据来源、最后更新时间和是否实时。
- 参数表单区分常用参数和高级参数。
- 启停按钮必须根据真实状态和当前操作锁定，避免重复提交。
- 错误信息同时提供用户可读摘要和可展开的技术详情。
- 场景卡片必须直观显示“4090 做什么、3090 做什么”，并在切换前显示资源变化摘要。

### 8.13 现有监控 Web UI 集成

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-WEBUI-001 | 预置 NInfer 4090 UI、NInfer 3090 UI 和 LM Studio Web Monitor 三个集成项。 | P0 |
| FR-WEBUI-002 | UI 卡片分别显示“监控 UI 状态”和“模型后端状态”，不能将其中一个在线推断为另一个在线。 | P0 |
| FR-WEBUI-003 | “打开 UI”仅打开已在线的 UI，不得隐式启动或重启模型服务。 | P0 |
| FR-WEBUI-004 | “启动 UI”只启动监控 UI；“启动环境并打开 UI”必须展示将启动的模型服务并经用户确认。 | P0 |
| FR-WEBUI-005 | UI 离线时启动动作异步执行，健康检查通过后再启用“打开”按钮；失败时显示启动日志和根本错误。 | P0 |
| FR-WEBUI-006 | 对 loopback UI 提供管理系统同源代理路径，使局域网浏览器无需直接访问 WSL/Windows 的 `127.0.0.1`。 | P0 |
| FR-WEBUI-007 | 同源代理支持 HTML、CSS、JavaScript、相对资源、流式响应、SSE 和 WebSocket；不支持的能力必须在导入时提示。 | P0 |
| FR-WEBUI-008 | 默认在新标签页打开现有 UI，并提供返回管理系统的入口；仅在经过兼容性验证后允许 iframe 嵌入。 | P0 |
| FR-WEBUI-009 | LM Studio Web Monitor 的模型加载、卸载和日志查看属于高权限功能，仅管理员可访问。 | P0 |
| FR-WEBUI-010 | 通过代理发生的模型加载/卸载动作进入统一审计；绕过管理系统直接操作时，状态协调器标记为“外部变更”。 | P0 |
| FR-WEBUI-011 | 不在 URL、页面、日志、Referer 或审计中暴露现有 UI 的静态访问凭据；凭据只通过 secrets 引用注入。 | P0 |
| FR-WEBUI-012 | 现有 UI 的地址、启动脚本、健康检查和后端依赖均可配置并版本化。 | P0 |
| FR-WEBUI-013 | 检测并提示陈旧的静态 LAN 地址；生成可访问链接时使用当前绑定与受信任的请求主机配置。 | P0 |
| FR-WEBUI-014 | LM Studio UI 启动前检查 8765 端口、LM Studio CLI/API 可用性和防火墙影响；不得重复创建宽泛规则。 | P0 |

### 8.14 网络与安全

| 编号 | 需求 | 优先级 |
|---|---|---|
| SEC-001 | 初次启动默认只监听 `127.0.0.1`；管理员完成账号初始化后才能显式开启局域网监听。 | P0 |
| SEC-002 | 局域网模式必须启用登录认证，不允许匿名执行控制操作。 | P0 |
| SEC-003 | 密码使用强哈希保存，Cookie 使用 HttpOnly、SameSite，HTTPS 时启用 Secure。 | P0 |
| SEC-004 | 所有状态变更接口需要 CSRF 防护、权限校验和审计。 | P0 |
| SEC-005 | 不能向浏览器暴露 Docker Socket、宿主命令执行能力或密钥。 | P0 |
| SEC-006 | 服务端只允许执行管理员预先登记的适配器动作，不提供通用 Web Shell。 | P0 |
| SEC-007 | API 密钥存入操作系统密钥库或独立 secrets 文件，数据库只保存引用。 | P0 |
| SEC-008 | 支持通过反向代理配置 HTTPS；直接公网访问不在支持范围。 | P0 |
| SEC-009 | 登录和高风险接口需要速率限制；连续失败应记录安全事件。 | P0 |
| SEC-010 | Docker 控制权限按高权限能力处理，部署文档必须明确风险和最小权限建议。 | P0 |
| SEC-011 | Web UI 代理目标必须来自管理员批准的 loopback/内网白名单，禁止用户输入任意 URL，防止 SSRF。 | P0 |
| SEC-012 | 对可能显示提示词、输出内容和模型日志的现有 UI，默认禁止观察者角色访问，并在页面标注数据敏感性。 | P0 |
| SEC-013 | 代理层校验 Host、Origin 和路径，移除不应透传的认证头及上游安全敏感响应头。 | P0 |

## 9. 配置与数据存储

### 9.1 MVP 存储建议

- SQLite：场景、环境、模型、参数版本、操作任务、用户、审计索引。
- 配置导出：可读的 YAML 或 JSON，不含密钥。
- 指标：MVP 可使用 SQLite/本地时序文件；数据量超过目标后再引入 Prometheus。
- 日志：保留原始来源，管理器只保存索引或受控轮转副本。
- Secrets：独立权限文件或 Windows Credential Manager，不进入 Git。

### 9.2 数据要求

- 数据库 schema 必须可迁移和备份。
- 配置修改必须生成版本。
- 运行时发现状态不得覆盖用户配置。
- 导入/导出格式必须包含版本号。
- 备份恢复不能自动启动任何工作环境。

## 10. 高层架构约束

```text
局域网浏览器
    |
Web UI / HTTP API
    |
控制与状态协调服务
    |-- 场景编排器与切换状态机
    |-- Docker Compose 适配器
    |-- Docker 容器适配器
    |-- WSL systemd 适配器
    |-- HTTP/OpenAI 健康适配器
    |-- 受控 Windows ComfyUI 启动器
    |-- 通用 Windows 服务/进程适配器（P1）
    |
监控采集器 -- 日志聚合器 -- SQLite/指标存储 -- 审计日志
```

架构约束：

- 控制面与模型推理进程分离，管理器崩溃不能直接终止模型服务。
- 所有控制动作通过适配器接口，不在业务层拼接任意命令。
- 读取状态与执行修改分离；只读轮询不得产生外部状态变化。
- 单个适配器故障不能拖垮整个管理页面。
- 后端状态是事实来源，数据库状态仅用于配置和历史记录。
- 场景切换由单一编排器串行执行；同一时刻最多存在一个影响 GPU 分配的切换任务。

## 11. 非功能需求

### 11.1 可靠性

- 管理系统异常退出或升级时，不停止已运行的模型环境。
- 生命周期操作必须可重试、可追踪、幂等。
- 依赖不可用时给出明确错误，不无限等待。
- 管理器启动后在 30 秒内完成首轮环境发现；超时目标标记未知。

### 11.2 性能

- 局域网中普通页面首屏目标小于 2 秒。
- 实时监控变化到达界面的延迟目标小于 3 秒。
- 启停请求应在 1 秒内返回操作任务 ID，实际启动耗时异步展示。
- 监控采集在空闲状态下不得持续占用超过一个 CPU 逻辑核的 10%；最终门槛需通过实现验证。

### 11.3 兼容性

- 服务端目标环境：Windows 11 + WSL2 Ubuntu 22.04 + Docker Desktop。
- 浏览器：当前版本 Chrome、Edge，以及手机端现代浏览器。
- 不依赖固定 WSL IP；使用发行版名称和稳定的宿主访问方式。
- 不依赖 GPU 枚举序号。

### 11.4 可维护性

- 新增后端通过适配器扩展，不修改核心生命周期逻辑。
- 配置字段和 API 有明确 schema 与版本。
- 错误使用明确类型，并保留根本原因。
- 关键状态机、冲突检查和权限逻辑必须具备自动化测试。

## 12. MVP 验收标准

### AC-01 环境发现

- 能发现当前 23 个 Docker 容器，并正确区分 1 个运行、22 个停止。
- 能发现 `ninfer3090-ui.service` 的真实状态。
- 能识别 `127.0.0.1:3000` 为网易云音乐 API 进程，但在未导入对应环境时不自动接管。

### AC-02 NInfer 4090 监控

- 显示 `ninfer-4090` 正在运行、端口 8080、模型 `qwen3.8-27b`。
- `/health` 成功时显示就绪；仅容器运行但健康检查失败时不显示就绪。
- 显示 GPU 4090 的显存、负载、温度和功耗。
- 显示 NInfer slots、请求数和可用 metrics。

### AC-03 3090 前后端分离状态

- 3090 UI 运行而 `ninfer-3090` 停止时，页面明确显示“控制 UI 在线、模型后端离线”。
- 不把 UI 的 HTTP 200 当作模型服务健康。

### AC-04 启停安全

- 启动 3090 环境前检查完整 GPU UUID、显存、端口和模型路径。
- 停止 3090 实例不会自动修改 restart policy。
- 修改 restart policy 不会自动停止当前实例。
- 操作失败时显示具体错误并形成审计记录。

### AC-05 状态恢复

- 在不停止模型服务的情况下重启管理系统。
- 重启后重新发现 `ninfer-4090` 已运行，不重复创建或重启容器。

### AC-06 日志

- 可分别查看管理器、NInfer 4090、NInfer 3090 UI 和 Docker 生命周期日志。
- 日志支持实时更新、暂停和关键词过滤。
- 模拟包含 Authorization 的日志时，页面和下载内容均不出现密钥明文。

### AC-07 局域网安全

- 未登录用户不能读取主机详情、日志或执行启停操作。
- 登录后可从同一局域网浏览器使用管理系统。
- 未经初始化，系统不能直接监听全部网卡。
- 所有启停和配置修改均能在审计页面追溯。

### AC-08 不干扰现有服务

- 执行环境扫描、GPU 监控、日志读取和页面刷新期间，`ninfer-4090` PID/容器 ID 不发生变化。
- 未明确选择的环境不被启动、停止、重启或修改。

### AC-09 开发/agent场景识别与恢复

- 当 4090 上的 NInfer/Qwen3.8 就绪，且 3090 开发辅助服务符合模板时，页面显示“开发/agent场景：已激活”。
- 4090 NInfer 就绪但 3090 的 ComfyUI 生图/视频、ASR 或 TTS 配置缺失时，显示“开发/agent场景：部分激活”，并列出缺失组件。
- 从视频场景切换到开发/agent场景时，系统先等待或处理 H3/ComfyUI 队列，再停止 8000 H3、确认 4090 显存释放，最后启动并验证 NInfer 8080。
- 同一次开发/agent场景切换还必须启动并验证 SenseVoiceSmall 18090 与 IndexTTS 1.5 vLLM 6006；健康检查需精确核对模型/服务身份、3090 UUID 与 unit 的 `CUDA_VISIBLE_DEVICES=1`。`Type=simple` unit 报 active 后，管理器必须在 600 秒窗口内轮询严格健康，不能用一次 5 秒探测判定冷启动失败；管理器关闭请求不得中断已可能改变服务状态的就绪轮询，必须等有界结果后进入既有协调、回滚或恢复锁流程。停止或回滚前需确认 `active_requests=0`；随后由 Uvicorn 优雅关闭立即停止接收新连接，并最多等待 300 秒完成竞态进入的在途请求。systemd 停止预算为 330 秒，管理器固定适配器预算为 630 秒。
- 整个流程通过一次场景确认完成，不需要手工执行 PowerShell、Docker 或 WSL 命令。

### AC-10 视频制作场景切换

- 从开发/agent场景切换前，页面显示将停止 NInfer 8080、启动 ComfyUI 8000/H3、准备 ComfyUI 8001 辅助服务的完整计划。
- 有 NInfer 活跃请求时默认不立即停止，可选择等待排空；强制切换必须二次确认。
- 切换成功后，通过 `/system_stats` 和实际 GPU 指标确认 8000 使用 RTX 4090、8001 使用 RTX 3090。
- H3 工作流配置检查确认 `steps = 8` 且使用兼容的 H3 Turbo 8-step LoRA；检测到 4-step 配置时阻止切换并报告原因。
- 8000 或 8001 任一必需组件未就绪时，视频场景不能显示为完全激活。

### AC-11 场景切换失败与回滚

- 模拟 NInfer 已停止但 H3 启动失败，系统能够识别失败步骤并尝试恢复原开发/agent场景。
- 自动恢复成功后显示“切换失败，已恢复开发/agent场景”；恢复失败时显示当前真实服务/GPU 状态和逐步人工恢复指引。
- 回滚不修改任何服务的自动启动或 restart policy。
- 审计中能看到场景任务、每个子操作、原始错误及回滚结果。

### AC-12 3090 多模型资源安全

- 开发 ComfyUI 生图/视频、TTS、ASR，以及视频场景中的 TTS、ACE-Step、ASR 均有独立显存预算和驻留策略。
- 当多个模型的显存预算超过 RTX 3090 可用显存时，系统阻止同时加载并改为排队或按需换入。
- 未完成兼容性验证的模型组合不会被场景模板默认为“可同时运行”。

### AC-13 桌面脚本目录导入

- 扫描 `C:\Users\xu\Desktop\本地模型启动` 后发现 6 个 `.cmd` 和 1 个 `.lnk`，扫描过程中没有创建进程、容器、systemd task 或防火墙规则。
- 正确解析 `lmstudio监控.lnk` 指向 `D:\AIWork\lmstudio_web_monitor\run.bat`，并显示工作目录。
- 正确提取 4090 NInfer 的 API 8080、UI 8081、项目路径、健康检查和 Start/Stop/Status 候选动作。
- 将交互式脚本标记为“需要转换”，未经审核不能作为 Web 后台命令直接运行。
- 修改任一已导入脚本后，系统检测到校验值变化并要求重新审核，不自动替换受管动作。

### AC-14 现有监控 UI 启动与打开

- NInfer 4090 UI 在线时，局域网用户通过管理系统同源地址打开 8081 UI，不需要直接连接 WSL loopback。
- NInfer 3090 UI 在线但后端离线时，UI 中心和代理页面均明确显示“UI 在线、后端离线”。
- LM Studio Web Monitor 离线时，“打开 UI”不可用；执行“启动 UI”前展示 8765 端口、脚本和防火墙影响，健康检查成功后才允许打开。
- 打开 LM Studio Monitor 时，浏览器地址、Referer、管理器日志和审计日志中不出现其静态访问凭据。
- 用户仅点击“启动 UI”时，不加载 LM Studio 模型，也不启动 NInfer 模型后端。

### AC-15 脚本冲突识别

- 导入后识别 NInfer 4090 与 q27 共用 8080。
- 识别 vLLM 与 H3 ComfyUI 共用 8000，并同时竞争 RTX 4090。
- 识别 BF16 llama.cpp 与 LM Studio 默认 API 共用 1234，且 BF16 服务同时占用 4090/3090。
- 在存在冲突的服务运行时，启动预检默认阻止目标操作并列出具体端口、GPU 和占用服务。
- 检测到 NInfer UI 中历史 LAN IP 与当前主机 IP 不一致时显示配置陈旧告警，不生成错误的局域网链接。

## 13. 开发里程碑建议

### M0：只读原型

- 主机、GPU、Docker、systemd 和端口发现。
- 仪表盘、环境状态页和两种场景的实际状态推断。
- NInfer 健康、slots、metrics、ComfyUI `/system_stats` 和日志读取。
- 桌面脚本目录只读扫描、快捷方式解析和冲突报告。
- NInfer 8081/18031、LM Studio 8765 UI 的状态发现，不执行启动。
- 不提供任何状态修改能力。

### M1：受控生命周期 MVP

- Docker Compose、容器、WSL systemd 适配器。
- 受控 Windows ComfyUI 8000/8001 启动器。
- 现有 NInfer/LM Studio UI 的白名单启动器和认证同源代理。
- 已审核桌面脚本转换出的非交互适配器动作。
- 启动、停止、重启、两种场景一键切换、异步操作和审计。
- GPU/端口/依赖预检。
- 登录认证和局域网模式。

### M2：模型与参数管理

- 模型清单、环境模板和参数预设版本。
- 当前 NInfer 4090/3090、q27、ComfyUI 8000/8001 和 OpenMontage 配置导入。
- 配置差异预览和应用流程。

### M3：监控与运维完善

- 历史指标、告警、日志轮转、备份恢复。
- vLLM、llama.cpp 专用适配器，以及更细粒度的 ComfyUI 工作流耗时、产物和节点级监控。
- Windows 服务和受控原生进程适配。

## 14. 待确认事项

以下问题会影响最终范围，进入详细设计前需要确认：

1. MVP 是否只允许一个管理员账号，还是第一版就需要管理员/观察者角色？
2. “开启、关闭”是否仅指模型工作环境，还是还包括整台工作站的关机、重启和 Wake-on-LAN？本文暂按前者定义。
3. 除已明确纳入 MVP 的 ComfyUI 8000/8001 启动器外，Windows 原生 LM Studio、llama-server 等进程是否必须进入 MVP？
4. 是否需要第一版就支持模型下载、移动和删除？本文将其放在 P1。
5. 是否需要从公网或异地访问？本文只支持可信局域网，不包含公网暴露。
6. 指标和日志期望保留多久，以及可接受的最大磁盘占用是多少？
7. 除用户主动点击场景切换外，是否需要自动调度，例如按任务类型自动进入视频场景？本文暂不启用自动切换。
8. 开发/agent场景中 ASR/TTS 与 ComfyUI Krea2/H3 服务的入口、端口、模型文件及 GPU 绑定已固化；ComfyUI、ASR、TTS 同时加载模型时的峰值显存上限仍需按实际工作流验收。
9. q27、vLLM、3090 NInfer、3090 Qwen 和双卡 BF16 llama.cpp 是作为“其他手动环境”提供，还是需要成为开发/agent场景的可选子预设？
10. LM Studio Web Monitor 是保留独立 8765 监听并加固，还是仅允许通过管理系统同源代理访问？MVP 推荐后者。

## 15. 需求变更规则

- 本文是本项目需求基线。后续新增或调整功能时，必须同步更新本文档。
- 每项实现需求应关联对应的需求编号和验收标准。
- 未经确认的待确认事项不得被当作既定需求实现。
- 实际环境参数发生变化时更新“环境基线”，但不把动态运行状态写成固定产品规则。

## 16. 主要 UI 实现基线（2026-08-27）

- 已建立响应式主要界面：总览、工作场景、工作环境、现有 WebUI、资源监控、日志中心和脚本导入。
- 视觉方向为深色工作站控制台，使用冷灰底色和单一青绿色状态强调；桌面端突出双 GPU 分工，手机端改为纵向阅读并提供抽屉导航。
- 首次访问根据 `/api/v1/auth/status` 显示本机首次管理员设置或登录；密码不进入浏览器存储和日志，CSRF 明文只保存在页面内存。刷新页面时 `/api/v1/auth/me` 为有效会话追加一个 CSRF；同一会话最近 8 个令牌可供多个标签页并行使用，第 9 个起淘汰最旧令牌，数据库仍只保存哈希且会话删除时级联清理。
- 总览与资源监控不得使用随机数、硬编码快照或模拟曲线。快照约每 3 秒读取，历史约每 12 秒读取；CPU、内存、磁盘、Docker、GPU 名称/UUID/负载/显存/温度/功耗及曲线末端必须来自同一真实采样批次。
- 轮询必须在每轮完成后再调度下一轮。每个登录生命周期必须取消旧在途请求，并用生命周期代次及资源请求序号拒绝旧响应和旧 401 覆盖新登录；成功响应若不是合法 JSON 对象，应显示安全的结构化错误而非触发前端类型错误。
- snapshot、history、services、audit、discovery 分别维护失败次数、最近成功和过期提示，不得由任一接口成功清除其他接口的失败状态。所有百分比在进入显示模型时统一校验并限制到 0..100，非有限值按无数据处理。
- 总览保持双 GPU 主视觉：每张卡左侧为真实显存圆环，右侧为各自最近 15 分钟负载大曲线。GPU 少于两张时固定卡槽显示“未检测到”；多出的 GPU 在工作环境页最简列出，不得崩溃或覆盖前两张卡。
- 资源监控分别绘制 CPU、内存及每张 GPU 的负载、显存曲线。SVG 必须响应容器尺寸，支持空数据和单点；`aria-label` 包含当前值、峰值与趋势摘要。
- 页面显示最近采样时间；连续请求失败时保留最后好数据并显示离线重试，超过 12 秒显示“数据已过期”。`collector_errors` 按模块展示简短降级信息，不显示服务器内部 cause；CPU 温度 unsupported 显示“不支持”。
- Docker 容器/关键端口接入 `/services`，脚本发现与重新扫描接入 `/discovery/scripts`，最近事件和日志中心接入 `/audit`。所有服务器文本使用 `textContent`/DOM 节点插入，不以 `innerHTML` 注入。
- 场景切换、环境启停、WebUI 启动/打开、脚本批准导入没有安全后端动作时必须明确禁用或标记“尚未配置”，不得保留前端假切换、假进度或假成功状态。
- 保持深色工业控制台视觉与移动端抽屉导航；登录表单具有 label、错误提示、提交忙状态和键盘焦点，动画遵守 `prefers-reduced-motion`。

## 17. M0 持久化、认证与脚本发现实现约束（2026-08-27 增补）

- SQLite 使用按版本顺序执行的可迁移 schema，升级时补齐表/列并保留旧数据。数据库保存单管理员、有期会话、登录失败计数、只追加审计事件、脚本发现结果与每次扫描元数据；默认数据文件位于项目 `data/` 且不进入版本库。
- 首次管理员设置仅允许 loopback 请求。密码使用 PBKDF2-HMAC-SHA256、每用户独立随机 salt 和常量时间比较；会话仅在数据库保存 token 哈希，Cookie 使用 `HttpOnly` / `SameSite=Strict` 并可配置 `Secure`。
- 未初始化管理员时禁止绑定非 loopback 地址。登录请求限制输入长度，并对同一来源 5 分钟内连续 5 次失败的后续尝试返回限速错误与安全审计。
- 管理员完成初始化后，除 health、auth status、setup 和 login 外的 `/api/v1` 路由均需登录；除 setup/login 外的状态修改请求还需校验与会话绑定的 CSRF header。未初始化时，原有只读 API 仅供 loopback 验收。
- setup、login 成功/失败、logout 和每次脚本扫描记录时间、来源 IP、事件、结果与安全摘要；审计不得保存密码、会话 token 或 CSRF 明文。
- 脚本扫描只识别 `.cmd` / `.bat` / `.ps1` / `.lnk`，仅读取元数据与文本。`.lnk` 允许使用固定 PowerShell COM 查询目标、参数和工作目录，必须 `shell=False`、固定查询程序且有超时；不得运行任何脚本、快捷方式或它们引用的程序。
- 启动时扫描失败不得阻止管理器启动；文本解码或单个快捷方式失败必须作为该条目的结构化错误返回，不得中断其他条目。
- 每次扫描都持久化 `scan_id`、时间、目录、目录是否存在、条目数和结构化错误；不存在/空目录/0 条结果也必须保存，查询需区分“从未扫描”并在重启后恢复最近一次元数据。
- `.lnk` 查询命令参数必须完全固定，待解析路径只能通过最小化子进程环境中的专用变量传入；路径不得拼接到 PowerShell `-Command` 文本。扫描需限制单文件大小、条目数、快捷方式数和总时长，且不得阻塞 ASGI 事件循环。
- 发现结果中的参数、目标、工作目录、URL、异常及 PowerShell 输出在返回或持久化前统一脱敏；认证请求体需在 ASGI 层累计限制大小并拒绝未知字段，PBKDF2 使用有界后台线程并发。
- 登录失败、审计事件和活动会话均设置保留上限；登录限速检查/计数、首次 setup 判断/插入和逐版本迁移必须使用明确事务，并保证并发竞争或迁移失败时可稳定回滚重试。

### 12.4 生命周期控制实现约束（2026-08-27）

- 生命周期控制使用独立 `config/control.json`，默认文件不存在且全局关闭；`config/control.example.json` 只提供禁用模板。用户必须复制、逐项核对并显式启用，发现脚本不得自动转为执行配置。
- 生命周期实现 `wsl_systemd`、固定 root 语义的 `wsl_systemd_root`、`wsl_docker_compose` 与固定 Python 指纹的 `windows_comfyui_process` 四种强类型适配器。禁止通用 command、任意路径、Web Shell、Desktop GUI/脚本入口和浏览器传入命令；所有子进程使用固定参数数组、`shell=False`、隐藏窗口、超时、输出限长与脱敏。Windows stop 只允许终止完整 exe/cmdline/端口 owner 匹配的精确 PID。
- 环境只读状态与写控制授权必须分离：只要固定 adapter 已登记，即使 `configured:false`，也应只读查询并显示真实的运行、停止或未知状态；只有 adapter 缺失时才显示“未接入适配器”。环境写动作仍要求登录、CSRF、全局 `control_enabled=true`、环境 `configured=true`、动作列入 `allowed_actions`，并提交精确确认文本。默认关闭或配置缺失时返回明确错误码。
- 总览顶部和工作场景页标题区始终提供醒目的“一键切换”入口，工作场景页分别显示“开发/agent场景 / 视频制作场景”两个直接入口；场景卡片底部保留同功能按钮。点击入口先调用只读安全预检；有 blocker 时只展示具体原因，不提交切换，预检完全通过后才显示精确确认并执行。初始加载、后台恢复或预检阻断不得把已存在的场景定义误写成“尚未配置”。
- 环境和场景动作以 SQLite 异步 operation 执行，通过 SQLite 原子租约和数据库旁的进程级文件锁保证跨实例同一时刻至多一个控制任务。场景固定执行 `drain -> stop conflicts -> verify release/ports -> validate paths/disk/VRAM/profile/dependencies -> start desired -> strict verify`，每一步持久化；只有实际处于 running 的冲突环境才要求 stop/回滚配置，已由固定适配器明确确认 stopped 的冲突环境不得因其自身尚未通过启动验收而阻断另一场景。失败仅回滚本次实际改变且已显式授权逆向动作的步骤，不停止未列入 conflicts 的环境，也不回滚原本已运行目标。回滚到 running 必须恢复完整严格健康，回滚到 stopped 必须明确读到 stopped，否则记录 `rollback_failed`。
- operations 与 operation_steps 记录请求人、来源、目标、动作、前后状态、步骤、时间、结果和脱敏错误摘要，并与只追加 audit 同事务关联。取得独占恢复锁的新管理器会把遗留 queued/running operation 及 running step 同步标记为 interrupted，并写入恢复审计；不得中断另一仍持锁实例的活跃任务。
- development 必需 4090 NInfer/UI 与 3090 ComfyUI 生图/视频、ASR、TTS；这五项必须全部位于场景 `desired`，不得用 `optional_desired` 绕过不完整环境。video 必需 H3 8000 与 3090 视频辅助 8001。任何必需环境、冲突状态适配器或健康检查缺失均阻断场景激活。默认模板因此必须明确阻断，不能显示假切换或假激活。
- `control.json` 缺失时 `control.example.json` 只能作为 `example_preview` 展示源，服务端无条件强制 `control_enabled=false`，不得继承示例中的启用值。健康检查只允许 adapter 状态、loopback TCP、allowlisted loopback HTTP 精确 JSON 字段、固定参数 NVIDIA GPU UUID/严格进程名、固定 WSL user unit 环境与 host GPU UUID/index 交叉核对，以及固定 WSL Docker Compose 的 DeviceRequests/环境/CUDA Driver 四层绑定核对。GPU AI 环境必须同时确认 adapter、端口/HTTP 模型响应和适配器对应的 GPU UUID 绑定；场景 active 与 operation succeeded 均以全部检查通过为前提。WSL/DXG 下 `nvidia-smi` 可能显示宿主全部 GPU，因此 Docker 运行时验收必须以 CUDA Driver 枚举 `count=1` 和目标完整 UUID 为准。
- `preflight_checks` 只允许强类型的活动请求 JSON/Prometheus drain、Comfy 两数组 queue drain、指定 GPU UUID 显存余量、绝对 WSL 路径、规范化本地盘符 Windows 路径及磁盘余量、已登记依赖环境健康、固定 loopback/WSL 内部端口冲突和 H3 静态 profile。Windows portproxy 已占用本机监听时，WSL 服务必须使用 `wsl_port_available` 检查发行版内部真实监听。Prometheus series 必须精确匹配 metric+labels 且登记值全部为 0；Comfy queue 缺字段、非 array 或非空均失败。Windows 路径拒绝 ADS、Win32 保留设备名及任一父级或目标 symlink/junction/reparse，`timeout_seconds` 必须由可终止隔离执行真实强制，超时 fail-closed 且不得留下阻止退出的执行单元。H3 必须严格为 8 steps、明确 8step-compatible LoRA、`shift_video=12`、`shift_audio=3`；任一缺失均为 blocker，不能 force。
- 环境 `start`/`restart` 不得通过直接调用动作 API 绕过预检：提交时和后台执行前必须复用同一套静态配置完整性校验，GPU AI 缺少 HTTP JSON 模型精确匹配或 GPU UUID 进程检查时分别拒绝创建 operation 或将已创建 operation 明确标记为 failed。为安全释放资源，显式列入 `allowed_actions` 的 `stop` 可在启动健康检查不完整时执行，但仍要求已核对的固定适配器入口。
- 单环境动作异常或超时后必须重新读取状态协调结果：目标已达到时，`start`/`restart` 仅在全部严格健康检查通过后记为 `succeeded/reconciled`，`stop` 已停止可直接协调成功；已发生状态变化但未达到目标或健康条件时，只能执行显式允许的逆动作并验证。状态无法判定、缺少安全逆动作或回滚验证失败时写入 SQLite 持久恢复锁，结果标记为 `recovery_required`/`rollback_failed`，阻断所有后续控制并在 UI 提示人工处理。管理员人工恢复到原状态后，必须通过登录态恢复预检、CSRF 和 `resolve-recovery:<environment-id>` 精确确认才能解除，解除动作写审计。
- 环境列表和预检必须返回逐动作 `action_capabilities`。普通非 AI 环境的 `stop` 不得因仅用于启动的健康检查缺失而禁用；可控 AI 环境必须具有与服务类型匹配的强类型 drain。执行 stop、restart 或失败后的 rollback-stop 前必须轮询活动请求/队列到 0，超时不得调用 adapter；restart 必须在清零后先停止并确认 stopped，再按冷启动门槛执行与 start 相同的资源、路径、依赖和 profile 预检，不能用运行中服务剩余显存冒充冷启动预算。普通 health 不能替代 drain。rollback drain 失败必须进入既有多环境恢复锁/人工恢复语义，不能以审计或恢复流程为由绕过。前端必须逐按钮采用后端 capability，并使用全局互斥 pending 防止重复确认覆盖；operation 轮询必须绑定当前登录生命周期的取消信号、generation 和 timer registry，旧生命周期不得刷新 UI 或弹出提示。
- NInfer4090 stopped 冷启动显存门槛固定为实测 47,000 MiB；Compose 使用 `restart: "no"` 防止 Docker/WSL 自动恢复绕过控制面，并给 SIGTERM 600 秒停止宽限。NInfer 当前没有 quiesce/拒绝新请求接口，因此 drain=0 与发出 stop 之间存在不可完全消除的竞态，验收不得称为完全无损。8080 保留既有 `0.0.0.0` 私有局域网发布且当前无 API key，只能部署在受信任 LAN 与主机防火墙边界，严禁公网暴露。
- 操作终态与恢复锁必须 fail-closed：只有 operation 终态已可靠写入，或 `recovery_required`/`rollback_failed` 与对应恢复锁在同一事务中可靠落盘，后台包装器才释放 SQLite 租约和进程文件锁。最终化数据库异常时当前实例进入 poisoned 状态、保留双锁且 shutdown 不主动释放；进程退出后新实例把遗留 environment operation 标为 interrupted/recovery_required，并生成明确 `running`/`stopped` 恢复锁后才允许人工恢复。原状态未知时恢复目标固定为安全的 `stopped`；任何非 `running`/`stopped` 的恢复目标均不可解除。状态 unknown 不得阻止显式允许且固定适配器完整的 stop。
- SQLite schema v9 使用 operation 关联的多环境 recovery items。场景保存每个环境的 before 状态与 running 严格健康基线；异常后任何 unknown/failed/非预期状态均按可能已变化处理。任一回滚失败、状态未知或健康未恢复都必须写匹配多环境锁，并持续阻断全局控制；新实例从遗留场景 operation 的 before/steps 重建锁。只有所有 item 达到明确 expected running/stopped（running 还需完整健康）后，管理员通过 CSRF 与 `resolve-recovery:<operation-id>` 精确确认才可整体解除。
- operations 页面展示持久化 steps 的顺序、阶段、目标、动作、当前状态、开始/结束时间、耗时、结果及脱敏错误；`failed`、`interrupted` 和 `rollback_failed` 必须醒目标示，轮询过程中持续更新。

## 18. WebUI、受限日志与 Windows/LAN 交付实现约束（2026-08-27 增补）

- 外部集成使用独立 `config/integrations.json`；默认正式文件不存在。`config/integrations.example.json` 只能作为 `example_preview`，服务端无条件强制所有示例条目 `configured:false`，不允许示例触发探测、代理或日志命令。
- WebUI 只接受已知固定 ID、`http/https`、字面 loopback IP 与显式固定端口；禁止 DNS 别名、用户信息、query、fragment、基础路径和浏览器传入目标。缺失或无效正式配置必须 fail-closed 并显示阻断原因。
- WebUI 可配置独立严格 `backend_probe`（固定 loopback HTTP(S) URL、有限 timeout、可选 JSON 点分字段精确标量匹配）。API 和页面必须分别返回/显示 UI 与模型后端状态；UI 在线而后端离线时仍可打开隔离只读预览，但必须醒目标警，不得整体误报。
- `/proxy/webui/{id}/...` 的首次 HTML 请求必须登录且仅允许 GET/HEAD 隔离只读预览；HTML 强制 CSP `sandbox allow-scripts` 与 `default-src 'none'`，只对当前管理器 origin 开放必要的 script/style/img/font/connect 与精确 `base-uri`，`form-action`/`frame-ancestors` 为 none，且禁止 allow-forms/allow-same-origin/allow-top-navigation。因 opaque sandbox 不携带管理器会话，静态资源改写到高熵短期只读 capability 路径：仅内存保存 hash、固定 WebUI ID、会话 hash 与 120 秒有效期，数量有界，每次读取必须同时确认 owner 会话仍有效，登出撤销；capability 不得写审计或管理器/外部反代访问日志。关闭环境代理和自动重定向，限制响应大小、并发和超时，过滤 Cookie/Authorization/CSRF/hop-by-hop 等敏感头。路径拒绝绝对 URL、反斜杠、控制字符及任意层编码后的 `..`。仅同一上游 origin 的 `Location` 可基于本次上游请求 URL 改写到隔离资源路径，外部跳转失败且错误不得泄露内部目标。
- 当前 MVP 不支持 POST、Cookie、WebSocket、SSE 或完整 SPA 交互；页面和文档必须称为隔离只读预览。相对静态资源、常见根绝对 src/href/action 与普通 query 应可用。
- 外部日志来源只允许固定容器名的 `docker_logs` 与固定 distro/scope/unit 的 `wsl_journal`。所有子进程使用固定参数数组、`shell=False`、隐藏窗口、超时、行数/时间窗口/输出上限、ANSI 清理与脱敏；禁止任意命令、路径和 grep。示例来源默认关闭，读取成功与失败均需审计。
- 管理器日志写入 `logs/manager.log`，默认 INFO、5 MiB、5 个备份；管理器日志作为固定管理员来源读取时同样限幅和脱敏。Cookie、Authorization、CSRF、密码和完整敏感 query 不得写入日志。
- WebUI 页面只在 `configured+online` 时允许同源打开或新标签打开，不得把远端浏览器指向 `127.0.0.1`；日志页提供固定来源、刷新、行数与自动刷新，同时保留 operations 和 audit。
- Windows 启动脚本必须与当前目录无关、支持可选设置文件并明确失败；计划任务安装/卸载脚本只供用户手动运行，使用当前用户、固定工作目录和精确任务名，不保存密码、不修改 AI 服务或防火墙。
- 默认继续监听 `127.0.0.1`。LAN 部署必须先本机完成 setup，再由管理员显式选择 `0.0.0.0` 或固定 LAN IP；文档只提供 Private profile、固定端口和可信子网的手动防火墙步骤，并说明 HTTPS 反代与 `Secure` Cookie。程序不得自动改防火墙。
- HTTPS 反代必须在完成本机 setup 后禁用 setup，并通过默认空、无通配符的 `allowed_public_origins` 精确声明外部 HTTPS origin、以 `trusted_proxy_ips` 声明实际 TCP proxy peer；代理保留 Host、阻断 setup，管理器不得从转发头推导客户端身份，未信 peer 携带转发头必须失败关闭。
- `/api/v1/health` 返回版本、API/integration schema 与不含路径、目标或底层 cause 的 readiness 摘要。全部网络与日志命令测试使用 mock；验收不得探测真实 integration 目标、执行真实日志命令或启停既有服务。

## 19. 交付状态矩阵与增补优先级（2026-08-27）

本节为当前实现状态的权威增补；若早期章节、表格或 P0 描述与本节冲突，以本节为准（supersede）。本轮交付不表示所有早期 P0 均已完成。

| 能力 | 当前状态 | 说明 |
|---|---|---|
| 登录、CSRF、审计、只读采样与脚本发现 | 已实现 | 仍受本文安全边界和限额约束。 |
| WSL systemd / Docker Compose 强类型生命周期适配器 | 已实现并默认关闭 | 只有正式控制配置、严格健康检查与人工确认后才可用。 |
| 强类型场景安全预检与多环境恢复锁 | 已实现并默认关闭 | drain、显存/端口、WSL 路径/磁盘、依赖、H3 profile 均需现场真实配置；失败锁定到逐项人工恢复。 |
| WebUI HTTP GET/HEAD 隔离预览 | 已实现并默认未配置 | 不是完整交互代理。 |
| WebSocket、SSE、POST、上游 Cookie 会话代理 | 延期 | 当前明确拒绝；不得以“WebUI 已接入”暗示支持。 |
| Windows ComfyUI 固定进程生命周期适配器 | 已实现；开发 Krea2/H3 8189 已配置 | 只接受固定 Python/main.py/路径/端口/GPU 指纹；8189 固定 RTX 3090，启动前交叉核对 host index 1 与目标 UUID，使用独立用户目录/数据库，并检查 Krea2 核心加载/采样节点、MiniMax H3 三个专用节点和 VHS 视频输出节点。状态交叉 PID、完整命令行和端口 owner，stop 先严格 queue drain 且只终止精确 PID。Desktop GUI、任意 Windows 进程和计划任务控制仍不支持。 |
| root systemd、Prometheus/Comfy drain、Windows 路径与 Comfy 能力探针 | 已实现 | `wsl_systemd_root` 固定 `-u root`；Comfy 能力同时核对 host GPU UUID/name/index、内部 cuda:0、队列结构和必需节点。配置证据不足仍必须 `configured:false`。 |
| NInfer 4090 Compose 与持久 UI | 已实现并通过现场验收 | Compose 唯一固定 4090 UUID，CUDA Driver 只枚举一张 4090；模型/视觉身份、真实聊天请求和 UI 持久 unit 均已验证。 |
| 模型、Profile、场景模板的 CRUD 管理 | 延期 | 当前仅消费严格本地配置文件，不提供浏览器编辑或任意导入。 |
| 任意日志路径、grep、命令控制台 | 不计划提供 | 与固定白名单、最小权限目标冲突。 |

后续增补必须在此矩阵中逐项改变状态并提供对应安全测试和验收证据，不能仅以旧 P0 标签宣称完成。

- 场景与环境列表属于只读状态发现：单个 WSL systemd / Docker Compose 状态命令最长等待 5 秒，不得继承模型冷启动所需的长动作超时；Web 控制状态请求最长等待 30 秒，避免高负载时被通用 8 秒请求上限误判为“未配置”。
- 总览 GPU 1 服务区必须从正式环境状态同步展示 SenseVoiceSmall 与 IndexTTS 1.5；服务停止且 start 动作预检可用时显示“已停止 · 可启动”，不得继续显示“ASR/TTS 规划未配置”。
- 工作环境页必须明确区分“只读状态发现”和“控制授权”：已登记固定适配器但尚未通过安全验收的环境可以显示真实运行状态，但不得显示可用控制动作；完全没有固定入口的环境显示“未接入适配器”。
